apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Prod inherits the high-availability defaults from base.
# You can add prod-specific patches here (e.g., DNS, certificates, higher resource quotas).
